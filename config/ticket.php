<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | Purchase hold TTL
    |--------------------------------------------------------------------------
    |
    | 仮確保（hold）は確定しないとこの秒数で期限切れになり、残席は待機列の
    | 先頭へ FIFO で回る。デモの既定は 3 分。確定済み枠はこの TTL の対象外。
    |
    */
    'hold_ttl_seconds' => (int) env('TICKET_HOLD_TTL_SECONDS', 180),

];
