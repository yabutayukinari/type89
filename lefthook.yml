pre-commit:
  parallel: true
  commands:
    pint:
      glob: "*.php"
      run: ./vendor/bin/sail bin pint {staged_files}
      stage_fixed: true

pre-push:
  commands:
    composer-build:
      run: ./vendor/bin/sail composer build
